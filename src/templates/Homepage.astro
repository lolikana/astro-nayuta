---
import { Access, FloorsMapList, Sns, Top } from '@components/homepage';
---

<Top />
<section class="section-template">
	<Sns />
	<FloorsMapList />
	<Access />
</section>

<style lang="scss">
	@import '@styles/utils/variables';
	@import '@styles/utils/mixins';
	.section-template {
		display: flex;
		flex-direction: column;
		gap: 170px;

		@include mq(md) {
			gap: 200px;
		}
	}
</style>
