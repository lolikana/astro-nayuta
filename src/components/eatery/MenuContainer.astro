---

---

<h2 class="menus__title">MENU</h2>
<div class="menus__container">
	<div class="menus__container_wrapper">
		<slot />
	</div>
</div>

<style lang="scss">
	@import '@styles/config';

	.menus__title {
		font-size: $__font11;
		text-align: left;
	}

	.menus__container {
		position: relative;
		&::before,
		&::after {
			content: '';
			width: 100%;
			height: 1px;
			background-color: $__primary;
			position: absolute;
			left: 0;
		}
	}

	.menus__container_wrapper {
		padding: 30px 0 80px;

		@include mq(md) {
			padding-top: 50px;
		}
	}
</style>
