---

---

<h2 class="menus__title">MENU</h2>
<div class="menus__container">
	<ul class="menus__container_items">
		<slot />
	</ul>
</div>

<style lang="scss">
	@import '@styles/config';
	.menus__title {
		font-size: $__font11;
		text-align: left;
	}

	.menus__container {
		max-width: 420px;
		position: relative;
		&::before,
		&::after {
			content: '';
			width: 100%;
			height: 1px;
			background-color: $__primary;
			position: absolute;
			left: 0;
		}
	}

	.menus__container_items {
		padding: 30px 0 80px;

		@include mq(md) {
			padding-top: 50px;
		}
	}
</style>
